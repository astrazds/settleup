# Use opaque Event Link tokens

SettleUp will use opaque random tokens in Event Links rather than human-readable slugs. Because the Event Link grants bearer access to view and edit an Event, unguessability matters more than pretty URLs; Events can still have human-readable titles without exposing those titles in the access token.
