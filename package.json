{
  "name": "settleup",
  "description": "No-login, private-by-link group expense splitter for one shared-cost event.",
  "homepage": "https://repos.astrazds.net/astrazds/settleup",
  "bugs": {
    "url": "https://repos.astrazds.net/astrazds/settleup/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://repos.astrazds.net/astrazds/settleup.git"
  },
  "keywords": [
    "cloudflare-workers",
    "d1",
    "expense-splitting",
    "hono",
    "settlement",
    "typescript"
  ],
  "author": "Andrejs Strazds <andrejs@astrazds.com>",
  "type": "module",
  "scripts": {
    "build:client": "node scripts/build-client.mjs",
    "predev": "npm run build:client",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "pretest:smoke": "npm run build:client && wrangler d1 migrations apply settleup --local",
    "pretest:smoke:critical": "npm run build:client && wrangler d1 migrations apply settleup --local",
    "pretest:smoke:extended": "npm run build:client && wrangler d1 migrations apply settleup --local",
    "test:smoke": "playwright test",
    "test:smoke:critical": "playwright test --project=critical",
    "test:smoke:extended": "playwright test --project=extended",
    "pretest": "npm run build:client",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "pretypecheck": "npm run build:client",
    "typecheck": "tsc --noEmit",
    "validate:html": "html-validate docs/design/mockups.html",
    "predeploy": "npm run build:client",
    "predeploy:dry-run": "npm run build:client",
    "deploy:dry-run": "wrangler deploy --dry-run --minify --outdir dist-dry-run",
    "verify": "npm test && npm run test:coverage && npm run typecheck && npm run test:smoke && npm run validate:html && sh -c 'trap \"rm -rf dist-dry-run\" EXIT; npm run deploy:dry-run'"
  },
  "dependencies": {
    "hono": "^4.12.23",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.1",
    "@types/react": "^19.2.15",
    "@types/react-dom": "^19.2.3",
    "@vitest/coverage-v8": "^4.1.7",
    "esbuild": "^0.28.0",
    "html-validate": "^11.4.0",
    "miniflare": "^4.20260526.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7",
    "wrangler": "^4.4.0"
  }
}
